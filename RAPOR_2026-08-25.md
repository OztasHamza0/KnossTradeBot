# Çalışma Raporu — 25 Ağustos 2026, 19:20–20:20

Bir saatlik oturumda dört commit atıldı. İşin tamamı **kart güvenliği** üzerine:
botun ürettiği işlem kartının gerçekten uygulanabilir ve ölçülebilir olmasını
sağlamak.

| Saat | Commit | Konu |
|---|---|---|
| 19:29 | `f5c272e` | Çoklu zaman dilimi + futures göstergeleri |
| 19:55 | `a72486e` | Kart güvenliği: birleşim kontrolleri |
| 20:07 | `e0a0b35` | Sessiz arıza sınıfı |
| 20:17 | `3878003` | Ölçüm doğruluğu + sonuç takibi |

**2192 satır eklendi, 54 satır silindi. Test sayısı 241 → 310.**

---

## 1. Çoklu zaman dilimi ve futures göstergeleri (19:29)

### Sorun

Bot AAVE %15 artmışken long önerdi. Coin 3 aylık tavanını test ediyordu ve
ertesi gün %7.5 düştü. Ardından tam tersine döndü ve her öneriye karşı çıkmaya
başladı.

İki davranışın kökü aynıydı: modelin elinde tek bir sayı vardı — 24 saatlik
değişim. Onunla "hareket başlıyor" ile "hareket bitti" ayırt edilemez.

### Kanıt

Aynı coin, aynı an, üç farklı pencere:

```
1 saatlik  (3 gün) : aralıkta %14   ← ucuz görünüyor
4 saatlik (20 gün) : aralıkta %80
günlük     (3 ay)  : aralıkta %86   ← 3 aylık tavana yapışık
```

### Eklenenler

Hepsi Binance'ten, ücretsiz, **yalnızca aday coinler için** çekiliyor:

- 3 gün / 20 gün / 3 ay aralık konumu
- RSI(14) ve ATR(14)
- Fonlama oranı, açık pozisyon değişimi, long/short oranı

Ölçülen etki: model artık *"RSI 76, üç pencerede de %96, ATR'ye göre risk/ödül
tutmuyor"* diye gerekçelendiriyor ve **fikrini neyin değiştireceğini** söylüyor.
Prompt maliyeti +470 token (çağrı başına +$0.002).

---

## 2. Kart güvenliği: birleşim kontrolleri (19:55)

Dış denetimin "para kaybettiren sınıf" bulguları. Ortak kök: `validateSignal`
her alanı **tek tek** denetliyordu, **birleşimlerini** hiç denetlemiyordu.

### A1 — Stop likidasyonun ötesine konabiliyordu

En tehlikelisi. Kaldıraç tek başına kurallara uygun, stop mesafesi tek başına
oynaklığa uygun, ama ikisi birlikte **stopun hiç çalışmadığı** bir kart
üretiyordu.

Canlı veriyle doğrulandı:

```
ONGUSDT  ATR %7.98  →  eski kontrol %8 ile %47.9 arası stop kabul ediyordu
                       10x'te likidasyon %9.5'te
```

Artık stop, likidasyon mesafesinin %60'ını aşamıyor ve red mesajı o stop
mesafesi için izin verilen **en yüksek kaldıracı** söylüyor.

### A2 — ATR denetimi dört yolun yalnızca birinde açıktı

Sadece elle `tara` yolunda. Otomatik nöbet, denetçi sonrası yeniden doğrulama
ve araştırma yollarında yoktu — yani kullanıcı başında yokken kontrol
gevşiyordu. Dördü de `analysisContext` üzerinden aynı sıkılıkta denetleniyor.

### A3 — Risk/ödül oranı kodda yoktu

Prompt'ta "en az 1:1.5" yazıyordu ama hiçbir kontrol yoktu; 1:0.67'lik kart
sorunsuz geçiyordu. Artık kodda, ve red mesajı o oranın gerektirdiği isabet
oranını söylüyor.

### A4 — Giriş fiyatı piyasa fiyatıyla karşılaştırılmıyordu

İki arıza birden:

1. **Bayat veri.** Piyasa verisi 15 dakikaya kadar eski sunulabiliyor; tek
   engel prompt'taki bir cümleydi.
2. **Çarpanlı parite hatası.** Doğrulandı:
   ```
   CoinGecko birim fiyat : $0.00000398
   Binance 1000PEPEUSDT  : $0.0039771     → 999 kat fark
   ```
   Araştırma modu prompt'a birim fiyatı koyup yanına "1000PEPEUSDT olarak
   işlem görüyor" yazıyordu.

Sapma %3'ü aşarsa red; büyük kat farklarında mesaj çarpanlı parite
karışıklığını işaret ediyor.

### Veri tarafı

- **3x kaldıraçlı ETF'ler** (SOXL, SOXS, KORU) filtreye takılmıyordu. 3x ürüne
  10x = 30x efektif maruziyet, "1x-10x" kuralı sessizce deliniyordu.
- **22/50 coine "1s +0.00%" yazılıyordu** — eksik veri değil, *yanlış* veri.
  Artık `null`; sert hareket tespiti ve aday seçimi bu coinleri "sakin"
  saymıyor, "bilinmiyor" sayıyor.

---

## 3. Sessiz arıza sınıfı (20:07)

Botun bozulup kimsenin fark etmediği durumlar.

| Bulgu | Düzeltme |
|---|---|
| Gönderim patlasa da `sent_signals` yazılıyordu → kart telefona gelmiyor ama 4 saatlik filtre yanıyor | `sendMessage` artık `boolean` dönüyor; kayıt sadece teyitli gönderimde |
| Denetçi düz metinle reddedince o metin **onay yorumu** olarak karta yapıştırılıyordu | Red dili tanınıyor → kart eleniyor; belirsiz metin onay gerekçesi gibi sunulmuyor |
| `bakiye 1,000` → **1 USDT** kaydediliyor, kill switch aniden devreye giriyordu | `parseAmount` binlik ayracını ayırt ediyor |
| Allowlist yoktu — botu bulan herkes saatlik nöbette kullanıcının LLM kotasından tarama alıyordu | `ALLOWED_CHAT_IDS`; yetkisiz sohbet `active_chats`'e **hiç** kaydedilmiyor |
| `force` yolu "nöbet kapat"ı es geçiyordu | Aralığı ve sessiz saati atlıyor ama `scan_enabled`'ı atlamıyor |
| `nöbet test` kart gönderip kaydetmiyordu → aynı kart dakikalar sonra tekrar geliyordu | Kaydediyor |
| Tekrar filtresi ters yönü engellemiyordu (LONG 10:00, SHORT 10:30 mümkün) | Aynı paritede 1 saat ters yön engelleniyor |
| CoinGecko yedeğine düşünce parite geçerlilik kontrolü tamamen kapanıyordu | Son bilinen liste saklanıyor, yedekte de denetleniyor |

---

## 4. Ölçüm doğruluğu ve sonuç takibi (20:17)

### C12 — `rangePosition` kendi kendine referanslıydı

Güncel fiyat, içinde konumlandığı pencerenin tepesine kendisi katkı yapıyordu.
Her yeni zirve otomatik %100 → **yerleşik bir tavana dayanmak ile o tavanı
kırmak aynı sinyali veriyordu.** Biri direnç, diğeri kırılım; zıt anlamlar.

Aralık artık kapanmış mumlardan kuruluyor, dışarı taşma kırpılmıyor, ayrı bir
"KIRILIM" etiketiyle bildiriliyor.

### C13 — `atr()` düz ortalamaydı

Kapanmamış mumu içeriyordu ve tek bir uç mum 14 bar boyunca eşit ağırlıkta
taşınıp sonra aniden bırakılıyordu. Bu değer sert bir kabul/red eşiğini
besliyor. Wilder yumuşatmasına geçildi; RSI ve ATR yalnız kapanmış mumlardan,
güncel fiyat kapanmamış mumdan.

### B7 — Sonuç takibi

`sent_signals` yalnızca kartı kaydediyordu, sonucunu değil. **"Bot işe yarıyor
mu" sorusunun ölçülebilir cevabı yoktu** ve bot aylarca zarar ettiriyor olsa
veride bunu gösterecek hiçbir şey olmazdı.

Eklenenler:

- Kartlar stop/hedef/kaldıraçla kaydediliyor
- `OutcomeTracker` 10 dakikada bir açık kartları 5 dakikalık mumlarla kontrol
  ediyor: fiyatın **önce hangisine dokunduğu sırayla** okunuyor. Anlık fiyata
  bakmak yetmez — önce stopa dokunup sonra hedefe giden işlem zararla
  kapanmıştır.
- Aynı mumda ikisi de görülürse sıra bilinemez → **karamsar varsayım (stop)**.
  Kendi performansını olduğundan iyi göstermek, yanlış bir stratejiyi
  sürdürmenin en kolay yolu.
- 72 saat sonra hâlâ açık kart `expired`
- **`performans` komutu**: isabet oranı **ve** R toplamı. %70 isabetle de para
  kaybedilebilir; iki sayı birlikte verilmezse karne yalan söyler. 20'den az
  kapanmış işlem varsa "bu sayı istatistiksel olarak anlamlı değil" uyarısı.

---

## Doğrulama

Bulguların hepsi canlı veriyle teyit edildi, düzeltmeler de canlı veriyle test
edildi:

```
10x + %20 stop        → RED: "fiyat stopa varmadan pozisyon likide olur"
risk/ödül 1:0.67      → RED: "isabet oranın %60'ın üzerinde olmak zorunda"
1000PEPE birim fiyat  → RED: "çarpanlı parite fiyatı karışmış olabilir"
3x ETF'ler            → top50'den ve tradablePairs'ten tamamen çıktı
1s verisi             → 22/50 artık dürüstçe null
```

Yeni saf fonksiyon modülleri ve testleri:

| Dosya | İçerik | Test |
|---|---|---|
| `indicators.ts` | RSI, ATR, aralık konumu, stop mesafesi | 30 |
| `risk.ts` | Likidasyon, risk/ödül, giriş sapması, pozisyon boyutu | 27 |
| `outcome.ts` | Sonuç kararı, R katsayısı, performans özeti | 20 |

Migration Neon'a uygulandı, `sent_signals` yeni kolonları yerinde.

---

## Açık kalan

**A5'in yarısı** — pozisyon boyutu hâlâ riske değil, margin tavanına bağlı.
`suggestedMargin` yazıldı ve test edildi ama prompt'a bağlanmadı; toplam
maruziyet sınırı da yok. İşlem başına gerçek risk hâlâ stop mesafesine göre
savruluyor.

Bunu eklemeden önce bir süre canlıda izlemek daha mantıklı — özellikle
`performans` verisi birikmeye başlasın.

---

## Senin yapman gerekenler

1. **`ALLOWED_CHAT_IDS`'i Render'a ekle.** Kendi chat id'ni loglardan
   görebilirsin (`Webhook received: {"message":{"chat":{"id":...`).
   Eklemezsen bot herkese açık kalır.

2. **Deploy'un tamamlandığını doğrula.** Açılış logunda:
   ```
   LLM: karar=... nobet=... , dusunme=low
   ```

3. **`performans` yaz.** Şu an boş çıkacak — eski 7 kartın stop/hedefi
   kaydedilmemişti, ölçüm bugünden başlıyor. Anlamlı bir karne için 30-50
   işlem gerekiyor.

---

## Kapanış notu

Bu oturumda yapılan hiçbir şey botu **kârlı** yapmaz. Yapılan şey, ürettiği
kartın uygulanabilir olduğunu garanti etmek (stop gerçekten çalışsın, giriş
gerçek fiyattan olsun, kaldıraç likidasyonu davet etmesin) ve sonucunun
ölçülebilir olmasını sağlamak.

İkincisi birincisinden önemli: `performans` verisi birikince "bu bot işe
yarıyor mu" sorusunun ilk kez dürüst bir cevabı olacak. Cevap "hayır" da
çıkabilir — ve bunu bilmek, bilmemekten iyidir.
